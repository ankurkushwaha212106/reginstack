#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    getline(cin,s);
    vector<string>ans(10);
    string temp;
    int count =0,index =0;
    while(index < s.size())
    {
        if(s[index] == ' ')
        {
            
        }
    }
}